#include <bits/stdc++.h>
using namespace std;

int main() {
  // 종료 조건 <- 무한히 하지 않게 한다. + 사이클이 생기면 안된댜. 
  // 반복문으로 될 거 같으면 반복문으로 풀자
  // 로직
  // 호출 종료

  // 점화석이 발현된다면 재귀함수로 간다.

  cout << 1 << "\n";
  return 0;
}